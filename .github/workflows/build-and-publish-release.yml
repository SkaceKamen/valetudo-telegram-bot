name: Build and release
concurrency: 
  cancel-in-progress: true
  group: build-and-release

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    name: Build
    strategy:
      fail-fast: false
      matrix:
        architecture: [amd64, arm64, arm]
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.21'

    - name: Build
      run: env GOOS=linux GOARCH=${{ matrix.architecture }} go build -o valetudo-telegram-bot ./cmd/valetudo-telegram-bot/main.go

    - name: Create release archive
      run: tar -czf valetudo-telegram-bot-${{ matrix.architecture }}.tar.gz valetudo-telegram-bot

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: valetudo-telegram-bot-${{ matrix.architecture }}
        path: valetudo-telegram-bot-${{ matrix.architecture }}.tar.gz

  release:
    name: Release
    needs: [build]
    runs-on: ubuntu-latest
    permissions: write-all
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: builds
          pattern: valetudo-telegram-bot-*
      - name: Create random tag
        uses: actions/github-script@v7
        id: tag
        with:
          result-encoding: string
          script: |
            const tag = Math.round(Date.now()/1000)

            github.rest.git.createRef({
              owner: context.repo.owner,
              repo: context.repo.repo,
              ref: 'refs/tags/' + tag,
              sha: context.sha
            })

            return tag
      - name: Publish release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ steps.tag.outputs.result }}
          files: builds/**/*